'''
Jess,

In your ann instance and util instance, you should not have 'gas' directory.

So, you can copy your config file from web instance, and you can import it onto

results_notify script :: e.g. config = Config()

                         AWS_REGION = config.AWS_REGION_NAME

- Stan

'''
